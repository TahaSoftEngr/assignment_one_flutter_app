import 'dart:async';
import 'package:flutter/material.dart';
import 'package:assignment_one_flutter_app/Home.dart' ;

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home:Home() ,
      //Test
    );
  }
}